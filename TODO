[-] turn on firewall by default
[-] automatically install vim plugins after linking dotfiles
[-] store iterm config in dotfiles
[-] store firefox config in dotfiles
[-] note that user needs to install xcode
	- do this in bootstrap.sh, can we check if xcode is installed
	- put it in the README
